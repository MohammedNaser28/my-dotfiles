Generate a GitHub Actions workflow for this project. Detect the language from the files present. For Go: lint (golangci-lint), test, build. For Rust: clippy, test, cross-compile to musl if it's a CLI tool. For TypeScript/Next.js: lint, typecheck, build. Include: caching (Go modules / cargo registry), matrix build for linux/amd64 and linux/arm64 if it's a CLI, and a release job that uploads artifacts on tag push (v*). Output as `.github/workflows/ci.yml`.
